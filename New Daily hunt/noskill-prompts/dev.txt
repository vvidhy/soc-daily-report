FIELD FALLBACK RULE (global, mandatory): If any named/parsed field returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message field: message:*<token>* (Lucene wildcard). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

Run a NOSKILL DEV-GL hunt for the last 24 hours - this single Sonnet hunt covers the ENTIRE DEV-GL (development/QA) environment, so the other hunts skip DEV-GL. HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (the Skill tool is disabled). Query ONLY the DEV-GL Graylog via the DEV-GL MCP. STEP 1: Read D:\Vidhya\New Daily hunt\streams.json and use ONLY the DEV-GL block (title to id) to scope searches directly; do NOT call list_streams. NOTE: DEV-GL data-tier (Redis/RabbitMQ/PG_Bouncer/Elasticsearch) and infra (ESXi/VCenter/ESET/Securenvoy/MTHP_MTSAN) are ALREADY covered by the separate db and infra hunts - do NOT re-hunt those here. THIS hunt covers DEV-GL: LINUX, WINDOWS-RDP, IIS-WEB, SFTP, AZURE identity, NETWORK (firewall/switch/lb), and APP only. DEV is dev/QA and lower-stakes, so this is a triage sweep: a finding here is generally one tier LOWER than the same on PROD unless it is an active compromise. TURN BUDGET: You have a maximum of 12 turns. PRIORITY ORDER: IIS-WEB > WINDOWS-RDP > AZURE > LINUX > SFTP > NETWORK > APP. At turn 10, stop new queries and write the report immediately. Any unchecked source-type = REVIEW coverage-gap finding with a runnable paste-ready query. TOKEN + LATENCY BUDGET: every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per source-type, plus ONE drill-down of 10 rows or fewer per CONFIRMED HIGH only. Aggregate first. If a source-type aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding NOT ASSESSED this run - name the type - coverage gap, action re-run manually) and MOVE ON. NEVER mark a type clean just because it was clean before; a type not queried THIS run is a REVIEW coverage-gap. WHAT TO HUNT in DEV-GL (one narrow aggregate per type; tag each finding with the matching surface): LINUX (Rsyslog) surface=linux - sshd brute force, sudo abuse, reverse shell, cryptominer (xmrig/kdevtmpfsi/kinsing/sysrv), cron or authorized_keys persistence, passwd or shadow or sudoers tamper; allow-list the strings jndi and ceurufrd as known. WINDOWS-RDP (Winlogbeat) surface=rdp - use underscored fields winlogbeat_winlog_event_id and winlogbeat_event_data_*; 4625 failed-logon bursts by TargetUserName (more than 3), 4624 type-10 RDP logons, 1102 log clear, 4720 new account, 4728 or 4732 admin-group add, and Defender detections on the channel Microsoft-Windows-Windows Defender/Operational (event ids 1116 1117 5001 5007). IIS-WEB surface=iis - base filter filebeat_log_file_path containing inetpub plus the Rebex filebeatIIS stream; Status 401 burst by client, 404 fan-out by client, scanner user-agents, and the attack tokens union select / the script tag / the dot-dot traversal sequence / jndi matched on the raw message. SFTP surface=sftp - stream SFTP_cerebus; the phrase address is blocked aggregated by client IP (brute pressure), the phrases key exchange failed or SSL accept error (scanner probes), large uploads via the phrase Successfully stored file, suspicious or executable extensions. AZURE surface=azure - the DEV-GL Azure event Hub (casepoint.in tenant); risky sign-ins, MFA failure or bypass, impossible travel; use the misspelled fields azure_prob_app_disply_name OR azure_prop_app_disply_name and azure_prob_condi_access_status; suppress noise codes 70043 70044 50173 50140. NETWORK surface=firewall or switch or lb - Fortigate (action:deny spikes, port-scan fan-out, inbound to mgmt ports 22 or 3389 or 445 from external), SURAT_SWITCHES and YOTTA_SWITCH (link-flap, running-config change, repeated mgmt auth fail), KEMPLM (L7 timeout spikes, admin or certificate changes). APP surface=app - PlatformDEV, PlatformQA, FOIA [DEV/QA], OpexusDEV, OpexusQA, Dev_Vintasoft, QA_Vintasoft, Exolution, MTTFS: http 401 or 403 spikes by IP or user, 5xx bursts, mass export or download, admin or role or permission changes, injection markers; also the Password_expire stream for repeated forced resets or lockouts. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag Cato egress. The Nessus and Tenable scanners (owner Sundhar Rajan: MTNESSUS01 10.102.100.199, LDPTNESSUS01 10.101.100.160, LDNESUS01 10.250.100.77, AZHNESSUS01 10.220.100.199, plus 140.82.202.196 and 199.27.40.187) cap at LOW and tag sanctioned scanner, but escalate if a scanner host does something out of profile (data export, admin action, lateral move). Internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports-noskill\dev-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with one section per source-type checked, each on its own line: ## LINUX, ## WINDOWS-RDP, ## IIS-WEB, ## SFTP, ## AZURE, ## NETWORK, ## APP; under each note No findings or the hits. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per source-type if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (always the literal DEV-GL), surface (one of linux|rdp|iis|sftp|azure|firewall|switch|lb|app), finding (120 chars or fewer), evidence (100 chars or fewer - timestamps, IPs, counts), mitre (T-code list or empty), action (100 chars or fewer concrete next step), query (the exact Graylog or Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream and fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.
