FIELD FALLBACK RULE (global, mandatory): If any named/parsed field (e.g. action, srcip, dstport) returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message field: message:*<token>* (Lucene wildcard). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

Run a NOSKILL NETWORK infrastructure hunt for the last 24 hours across AZ-GL, PROD-GL and OP-GL only - DO NOT query DEV-GL (it is covered by the separate dev hunt; emit the ## DEV-GL section as Covered by the dev hunt, with no DEV-GL findings-json entries) - FIREWALL, SWITCHES and LOAD BALANCER only. HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (the Skill tool is disabled). STEP 1: Read D:\Vidhya\New Daily hunt\infra-streams.json and use ONLY the network category — it has three sub-keys: network.fortigate (FortiGate firewall), network.switch (switches), network.kemp (KEMP load balancer). Query the listed stream IDs directly, do NOT call list_streams; a GL not listed for a sub-key is Not Applicable there. TOKEN + LATENCY BUDGET (these streams are huge and slow to scan): every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per category per GL, plus ONE <=10-row drill-down per CONFIRMED HIGH only. Narrow index-friendly filters: FIREWALL (Fortigate, the slow one - keep to this single indexed clause) -> action:deny as PRIMARY (you may use up to 3 aggregates for FIREWALL, overriding the one-per-category cap): aggregate by srcip with fetchLimit 10000, and when truncated ALSO aggregate by dstport and run a per-IP volumetric on each top source so a single-source single-port flood (for example dstport:23 Telnet) is graded HIGH and never down-graded to MEDIUM just because the sample truncated. If action:deny returns 0 on a NON-empty stream (PROD-GL Fortigate does exactly this), do NOT mark it clean: aggregate by the action field to enumerate the real action values, also try action:drop OR action:blocked OR action:reject OR action:timeout, and emit a REVIEW finding 'deny logging absent on this GL Fortigate - only accept and close present, ingestion gap, escalate to Network Eng to enable deny logging or locate the deny stream' with the action breakdown as evidence; SWITCHES -> link-change OR auth-failure OR config-change; LOAD BALANCER (KEMP) -> the L7 TRAFFIC signature, NOT admin keywords (these KEMPLM streams are L7 connection logs, so admin keywords return zero). Use Lucene double-quote phrase syntax to match the phrase waiting for initial client request together with the phrase Connection timed out (the nodest backend token also marks every half-open), aggregate by source on dstport:443, and read attacker client IPs from the ip:port to VIP token inside each message; ALSO run login OR permission OR certificate for any admin or cert events. A zero on the admin query is NOT clean, only a zero on the L7 traffic query is. You may use up to 3 aggregates for the load balancer, overriding the one-per-category cap. If a category's aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding 'NOT ASSESSED this run - <category> coverage gap', action 're-run manually') and MOVE ON; NEVER record a slow or skipped category as No findings. WHAT TO HUNT: FIREWALL - denied/blocked spikes from one external IP, port-scan fan-out, IPS/IDS signature hits, geo-anomalous allows, VPN or admin auth failures, config changes, any inbound allow to mgmt ports 22/3389/445 from external IPs. SWITCHES - link up/down flapping, running-config changes, repeated mgmt auth failures, port-security violations, unexpected SNMP or admin access. LOAD BALANCER (KEMP) - admin logins and failures, virtual-service or rule changes, certificate changes, TLS handshake error spikes, health-check state flapping. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag 'Cato egress - likely false positive'; internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports-noskill\network-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with exactly 4 sections, one per environment, each on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL; under each note each category checked with No findings or Not applicable. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (one of fortigate|switch|kemp), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream/fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.