FIELD FALLBACK RULE (global, mandatory): If any named/parsed field (e.g. Client_ip, Size_In_MB, Origin_ip) returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message field: message:*<token>* (Lucene wildcard). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

STEP 0 — PRE-COMPUTED: Read reports-noskill\sftp-preparse.json before running any queries. JSON structure: top-level "envs" object with keys "PROD-GL" and "OP-GL" (AZ-GL has DTC only, no SFTP preparse). Each env object has: blocked_sources[] (ip, count, is_medium), blocked_source_total, tls_probe_count, auth_failure_count, large_transfer_count. Access as envs["PROD-GL"].blocked_sources etc. - blocked_sources[]: emit these directly as the mandatory blocked-source finding (no MCP query needed for this check). Any entry with is_medium=true (count > 300) = MEDIUM finding. - tls_probe_count > 0: emit TLS-recon finding with count as evidence. - auth_failure_count > 0: run targeted auth-failure drill. - large_transfer_count > 0: run drill on large file transfers (filter Size_In_MB > 500 in Claude query). If file missing or > 6h old: run "address is blocked" aggregate manually and mark REVIEW coverage-gap.

Run a NOSKILL SFTP and DTC hunt for the last 24 hours. HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (do not invoke sftp-graylog-hunt or any other skill; the Skill tool is disabled). STREAM IDs are pre-resolved in D:\Vidhya\New Daily hunt\streams.json (by environment, title to id) - read it once and use those IDs to scope your searches directly; do NOT call list_streams. Fall back to list_streams only for a stream missing from that file or if a listed ID returns zero rows. STREAM SCOPE — use these IDs directly, do NOT call list_streams:
SFTP (Cerberus): PROD-GL = External_SFTP_cerebus (6696b768cbc7125b47f1b972) + Internal_SFTP_cerebus (6298c1d54511b766a1852033); OP-GL = External_SFTP (69afd0150e2baec71fdf922a) + Internal_SFTP (69afd0420e2baec71fdf9253); AZ-GL = NO Cerberus SFTP streams; DEV-GL = SFTP_cerebus covered by dev hunt only.
DTC (Rebex): AZ-GL = DTC_Rebex (616813d3067ed03d6e7c1b2d); PROD-GL = DTC_Rebex_PROD (6149e2537a8c5b2399280cfd) + DTC_Rebex_CA (63e108dbdf8f37233e873254) + DTC_Rebex_PT (642d75005ffb1c339b41cb6c); OP-GL = NO DTC streams; DEV-GL = DTC_Rebex covered by dev hunt only.
AZ-GL has DTC only (no Cerberus SFTP) — run DTC checks on AZ-GL, emit ## AZ-GL with DTC findings or CLEAN. ALLOWLIST - these three are Casepoint Cato egress IPs, NOT external attackers: 140.82.202.196, 199.27.40.187, 123.253.153.138. When any appears as the client/source IP, cap severity at MEDIUM and tag the finding 'Cato egress - likely false positive'; do not silently drop it if a real attack pattern is present (webshell path, executable upload, credential filename), but never treat a Cato IP as a confirmed external threat. Cerberus SFTP message decoding: 'Successfully stored file at' = upload (client to server), 'Successfully sent file' = download (server to client), 'Key exchange failed' or 'SSL accept error' or 'wrong version number' = SSH/TLS scanner probe; the username appears after the IP in brackets and session id ties events together. Useful fields: Client_ip (remote IP), Origin_ip (ASN/geo string), Size_In_MB (0 means non-transfer event). Look for SFTP brute force, failed-login spikes, large transfers / exfil, suspicious file extensions, malware or executable drops, anomalous file-share behavior. TURN BUDGET: You have a maximum of 18 turns. At turn 14, complete your blocked-source and TLS-recon sweeps and write the report. If you hit turn 18, STOP and emit a REVIEW coverage-gap finding for any environment where brute-force, blocked-source, TLS-recon, or exfil checks were not completed. MANDATORY blocked-source sweep (this is what aggregation truncation hides): run the phrase address is blocked (Cerberus rejects blocklisted IPs at the TCP-accept layer; use Lucene double-quote phrase syntax) aggregated by Client_ip across ALL SFTP streams and report the top offenders with their reject counts even though they never authenticated - a high per-IP reject count (over 300 in 24h) is sustained brute-force pressure worth a MEDIUM finding plus a perimeter-block recommendation, NOT noise to drop. ALSO run a TLS-recon sweep for the phrases version too low OR SSL accept error OR wrong version number aggregated by Client_ip to catch vulnerability scanners that probe TLS and the login page but never authenticate. For auth failures, distinguish BURST (many failures within 60s from one IP = brute force, higher severity) from SCATTERED (spread over hours = low-and-slow or noise, lower severity); require a correlated second indicator before calling anything HIGH. Aggregate first; do not pull millions of rows. Do NOT generate the skill's own PDF - instead save a concise markdown report to reports-noskill\sftp-latest.md with findings, severity, evidence (timestamp, source IP, file, bytes) and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment, include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (sftp|dtc), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.