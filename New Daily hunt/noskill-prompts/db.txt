FIELD FALLBACK RULE (global, mandatory): If any named/parsed field (e.g. level, application_name, source) returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message field: message:*<token>* (Lucene wildcard or phrase). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

Run a NOSKILL DATA-SERVICES (DB) hunt for the last 24 hours across all four Graylogs - Postgres/pgbouncer, Redis, RabbitMQ and Elasticsearch/Opensearch only. HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (the Skill tool is disabled). STEP 1: Read D:\Vidhya\New Daily hunt\infra-streams.json and use ONLY the db category - query the listed stream IDs directly, do NOT call list_streams; a GL not listed is Not Applicable there (OP-GL db = Elasticsearch, with Opensearch as the live fallback). TOKEN + LATENCY BUDGET (these are the highest-volume, slowest streams - e.g. RabbitMQ ~2.3M/day): every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per stream per GL, plus ONE <=10-row drill-down per CONFIRMED HIGH only. Narrow index-friendly filters: RabbitMQ -> auth-failure / access-refused only; Redis -> the dangerous commands run AS SEPARATE single-term aggregates (CONFIG; then SLAVEOF; then FLUSHALL; then MODULE; then NOAUTH), up to 5 aggregates for Redis overriding the one-per-stream cap - NEVER combine them in one OR query, which exceeds maxClauseCount=4096 and errors out; a clause-limit error is NOT a coverage gap once the split queries run, report the per-command count where 0 means genuinely clean; also check NOAUTH and WRONGPASS auth errors; Postgres/pgbouncer -> FATAL auth or role errors only; Elasticsearch and Opensearch -> error OR FATAL free-text, trusted over a structured level-field query; if a level-field query returns 0 but free-text error OR FATAL returns thousands, use the free-text result and sample 5 raw messages to classify. If a stream's aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding 'NOT ASSESSED this run - <stream> coverage gap', action 're-run manually') and MOVE ON; NEVER record a slow or skipped stream as No findings. WHAT TO HUNT: auth failures / brute force against DB, queue or search, connection floods, Redis unauthenticated dangerous commands (CONFIG/SLAVEOF/MODULE/FLUSHALL), Postgres FATAL auth or role errors, error or FATAL spikes suggesting injection or scanning, config changes, unexpected admin access. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag 'Cato egress - likely false positive'; internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports-noskill\db-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with exactly 4 sections, one per environment, each on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL; under each note each stream checked with No findings or Not applicable. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (always the literal db), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream/fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.