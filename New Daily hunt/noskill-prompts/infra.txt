FIELD FALLBACK RULE (global, mandatory): If any named/parsed field (e.g. level, source, event type) returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message field: message:*<token>* (Lucene wildcard or phrase). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

Run a NOSKILL INFRA security-appliance hunt for the last 24 hours across all four Graylogs - EDR (ESET), MFA (Securenvoy), VIRTUALIZATION / OUT-OF-BAND MGMT (vCenter/ESXi/Dell-iLO/UCS), and HARDWARE / SAN health (DR_Hardware/Hardware/HARDWARE/MTHP_MTSAN) only. HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (the Skill tool is disabled). STEP 1: Read D:\Vidhya\New Daily hunt\infra-streams.json and use ONLY the eset, securenvoy, virt and hw categories - query the listed stream IDs directly, do NOT call list_streams; a GL not listed for a category is Not Applicable there. TOKEN + LATENCY BUDGET: every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per category per GL, plus ONE <=10-row drill-down per CONFIRMED HIGH only. Narrow index-friendly filters: EDR (ESET) -> threat/detection/quarantine/infiltration events only; MFA (Securenvoy) -> failure/denied/lockout only; VIRT and OOB -> login OR permission-change OR VM-lifecycle OR config-change, PLUS a dedicated ESXi sweep because non-vCenter root logins use the line Accepted password for user root which the login keyword misses (match the phrase Accepted password via Lucene double-quote phrase syntax excluding 127.0.0.1, plus the tokens Lockdown OR esxcli OR shell and the phrase SSH login enabled); and when the broad vCenter aggregate truncates over 1000 events, narrow to NoAccessUserEvent OR VmRemovedEvent OR clone OR export OR PermissionAddedEvent OR RoleAddedEvent and the phrase Cannot login, aggregated by event type, so failed logins and VM lifecycle ops are not buried in benign volume. You may use up to 3 aggregates for VIRT/OOB. NEVER inherit a prior-day verdict or skip a stream as was-clean-yesterday: a stream not queried THIS run is ALWAYS a REVIEW coverage-gap finding, never No findings. If a category's aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding 'NOT ASSESSED this run - <category> coverage gap', action 're-run manually') and MOVE ON; NEVER record a slow or skipped category as No findings. WHAT TO HUNT: EDR-ESET - ANY malware or threat detection, quarantine, real-time-protection-disabled, or threat-not-cleaned event is at least a HIGH candidate; report host, threat name, action, and whether it was cleaned. MFA-Securenvoy - auth failures, repeated denials, lockouts, token or seed errors, admin changes. VIRTUALIZATION-OOB - interactive or failed admin/root logins to vCenter/ESXi/iLO/UCS from unexpected IPs, VM create/delete/clone/export, datastore browse or download, account or permission changes, SSH enabled on ESXi. HARDWARE-SAN (hw category: DR_Hardware/Hardware/HARDWARE/MTHP_MTSAN) - one aggregate per GL on critical OR error OR fault OR fail OR login OR config-change; flag repeated hardware faults, BMC or SAN logins from unexpected sources, firmware updates, and LUN-mask or zoning or volume-map changes; a hw stream not queried this run is a REVIEW coverage-gap, never clean. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag 'Cato egress - likely false positive'; internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports-noskill\infra-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with exactly 4 sections, one per environment, each on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL; under each note each category checked with No findings or Not applicable. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (one of eset|securenvoy|virt|hw), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream/fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.