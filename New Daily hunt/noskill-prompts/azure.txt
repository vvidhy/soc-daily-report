FIELD FALLBACK RULE (global, mandatory): If any named/parsed field (e.g. azure_prob_user_principal_name, azure_prob_condi_access_status, azure_prop_resource_disply_name) returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message or full_message field: message:*<token>* (Lucene wildcard). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

STEP 0 — PRE-COMPUTED: Read reports-noskill\azure-preparse.json before running any queries. JSON structure: top-level "envs" object with keys "AZ-GL" (tenant=mycasepoint.com) and "PROD-GL" (tenant=casepoint.com). Each env object has: failed_signin_count, top_failed_users[] (upn, count), mfa_failure_count, high_risk_count. Access as envs["AZ-GL"].top_failed_users etc. - top_failed_users[]: your geo-ACL and impossible-travel drill candidates. Skip broad failed sign-in aggregate; drill these UPNs directly. - mfa_failure_count > 0: investigate MFA bypass candidates. - high_risk_count > 0: immediate HIGH candidate — run drill. If file missing or > 6h old: run failed sign-in aggregate manually and mark REVIEW coverage-gap.

Run a NOSKILL Entra/Azure identity hunt for the last 24 hours. HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (do not invoke the azure skill; the Skill tool is disabled); query the Azure Event Hub streams across environments via MCP. TENANT-TO-STREAM ROUTING (one stream per tenant): mycasepoint.com -> AZ-GL stream 65cf0501c0fccf5d65f5025f; casepoint.com -> PROD-GL stream 6979bcc8245e61e78824df15; casepoint.in / DEV-GL -> DO NOT query, it is covered by the separate dev hunt (emit the ## DEV-GL section as Covered by the dev hunt, with no DEV-GL findings-json entries); OP-GL has NO Azure stream - do NOT run ANY query against OP-GL for Azure (it would only waste tokens); simply emit the ## OP-GL section as Not applicable. FIELD NAMES are real misspellings emitted by the content pack - use them verbatim or queries return zero silently: azure_prob_app_disply_name (disply not display), azure_prob_condi_access_status (condi not condit), azure_prop_resource_disply_name; the parser emits both azure_prob_* and azure_prop_* prefixes so OR them together (e.g. azure_prop_user_principal_name OR azure_prob_user_principal_name). IGNORE these Casepoint Cato egress IPs (they are not attackers): 140.82.202.196, 199.27.40.187, 123.253.153.138. Token-replay tell: MFA satisfied by claim in token from a new IP. Note azure_prob_device_detail_is_compliant is broken (false for ~99.6% of sign-ins) - do not rely on it for device posture. Look for risky sign-ins, MFA failure/bypass, impossible travel, token replay, cross-tenant access, conditional-access anomalies, new admin/role changes. Suppress noise codes 70043, 70044, 50173, 50140. TURN BUDGET: You have a maximum of 18 turns. At turn 14, finish your current geo-ACL checks and write the report. If you hit turn 18, STOP and emit a REVIEW coverage-gap finding for any tenant (AZ-GL, PROD-GL) or check category (geo-ACL, impossible travel, MFA bypass) not yet completed. GEO-ACL per application (flag a sign-in to these apps from a country OUTSIDE the allowed set; identify the app via azure_prob_app_disply_name OR azure_prop_app_disply_name and the country via the sign-in location or country field): STANDARD ACL for eCase portal, PAL and ATIP-PAL, and FOIA portal and FOIA API allows United States, Virgin Islands US, American Samoa, Guam, Northern Mariana Islands, Puerto Rico, United States Minor Outlying Islands, Colombia, Ireland, Poland, United Kingdom, Singapore, Canada, Germany, Australia, Italy, Japan, Hong Kong, India, Romania, Mexico, Netherlands, Israel. EXCEPTION UT Prod and UAT eCase allows United States plus the six US territories ONLY (any other country is at least HIGH). EXCEPTION EPA Prod PAL is worldwide with no geo restriction, so do NOT raise a geo finding for EPA PAL. A SUCCESSFUL sign-in to a standard-ACL app from outside its allowed set is at least HIGH; a failed attempt is REVIEW. Aggregate first. Do NOT generate your own PDF or skill-native report - write ONLY the markdown file below, ending with the findings-json block. You MUST call the Write tool to SAVE the full report to reports-noskill\azure-latest.md (overwrite any existing file) BEFORE you finish - do NOT only print it to the screen; the task is NOT complete until that file exists on disk, and printing the report instead of writing it is a failure. Save the markdown report to reports-noskill\azure-latest.md with evidence and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment (e.g. Azure on OP-GL), include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (iis|rdp|azure|linux), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.