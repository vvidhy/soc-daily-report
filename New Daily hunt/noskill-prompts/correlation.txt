Run a NOSKILL CROSS-MODULE correlation and kill-chain reconstruction pass for today's no-skill hunt. DO NOT USE ANY SKILL (the Skill tool is disabled). Do NOT re-hunt raw logs broadly. INPUT: read reports-noskill\_merged-findings.json — a compact JSON array of every finding from all hunts (each object has sev, env, surface, finding, evidence, mitre, action). This is your ONLY input; do NOT read per-hunt markdown files. Build an entity index across ALL findings: source/client IPs, usernames/accounts, hostnames, and timestamps. GOAL: find attack chains spanning more than one surface or more than one environment. CORRELATE when two or more findings from DIFFERENT surfaces or envs share an entity (same IP, same username, or same host) OR cluster in a tight time window on the same host/env. For each correlation, reconstruct the kill chain in MITRE ATT&CK tactic order. ALLOW-LIST: Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 are shared corporate egress — never anchor a kill chain on them. CONFIRMATION (optional, bounded): you MAY run at most 5 targeted aggregate_logs queries to confirm a HIGH-confidence suspected link — aggregate only, never raw rows. Do NOT run any live query against DEV-GL; for DEV-GL entities put the confirmation query in the investigate field only, marked unverified. SEVERITY: confirmed multi-stage cross-surface chain = HIGH; plausible but unconfirmed shared-entity link = REVIEW; weak or coincidental overlap = LOW. If no cross-surface or cross-env chain exists, say so explicitly. You MUST use the Write tool to SAVE the report to reports-noskill\correlation-latest.md before finishing — printing instead of writing is a failure. Structure the markdown with: '## Kill Chains' (one subsection per chain: ordered MITRE stages, shared entity, contributing findings by surface and env, confidence level), '## Correlated Entities' (IPs/users/hosts appearing in more than one finding), '## Cross-Check Coverage' (entities cross-checked and what was ruled out). After those sections append a fenced code block: three backticks then findings-json, a valid JSON array, then closing three backticks. One object per chain (include one CLEAN object if no cross-surface chain was found). Include LOW and REVIEW entries. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (environments involved, e.g. OP-GL or OP-GL+AZ-GL), surface (always the literal string correlation), finding (<=120 chars), evidence (<=100 chars — shared entity + contributing surfaces + timestamps), mitre (T-code list or empty), tactic (primary MITRE ATT&CK tactic, e.g. TA0011 Command and Control), killchain (furthest Lockheed stage reached — exactly one of Reconnaissance|Weaponization|Delivery|Exploitation|Installation|C2|Actions), action (<=100 chars concrete next step), query (exact Graylog query you actually ran), investigate (Graylog query for the analyst to run next — pivot on IP/user/host, name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.
