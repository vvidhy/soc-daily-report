FIELD FALLBACK RULE (global, mandatory): If any named/parsed field (e.g. application_name, auditd_exe, auditd_uid, source) returns null or zero results on a GL, DO NOT skip the check. Re-run the same detection using the raw message field: message:*<token>* (Lucene wildcard). Only emit REVIEW and move on if the raw message search ALSO returns no data. "Parsed field absent" is NEVER a terminal verdict when raw messages exist on that stream.

STEP 0 — PRE-COMPUTED: Read reports-noskill\linux-preparse.json before running any queries. JSON structure: top-level "envs" object with keys "AZ-GL", "PROD-GL", "OP-GL". Each env object has: ssh_failures[] (ip, count), ssh_failure_total, threat_count, sudo_count. Access as envs["AZ-GL"].ssh_failures etc. - ssh_failures[]: drill ONLY these IPs for SSH brute force (no new failure aggregate needed). - threat_count > 0: run targeted drill for cryptominer/reverse-shell signatures on that GL. - sudo_count > 0: run drill on sudo/su events. If file missing or > 6h old: run ssh failure aggregate manually and mark as REVIEW coverage-gap.

Run a NOSKILL Linux hunt for the last 24 hours across AZ-GL, PROD-GL and OP-GL only - DO NOT query DEV-GL (it is covered by the separate dev hunt; emit the ## DEV-GL section as Covered by the dev hunt, with no DEV-GL findings-json entries) (Rsyslog/syslog/auth/secure/audit streams). HUNT DIRECTLY VIA THE GRAYLOG MCP TOOLS ONLY - DO NOT USE ANY SKILL (do not invoke sd-linux or any other skill; the Skill tool is disabled). STREAM IDs are pre-resolved in D:\Vidhya\New Daily hunt\streams.json (by environment, title to id) - read it once and use those IDs to scope your searches directly; do NOT call list_streams. Fall back to list_streams only for a stream missing from that file or if a listed ID returns zero rows. Look for sshd brute force, sudo abuse, reverse shells, cryptominers (xmrig, kdevtmpfsi, kinsing, sysrv), cron/systemd/authorized_keys persistence, /etc/passwd|shadow|sudoers tampering. TURN BUDGET: You have a maximum of 18 turns. At turn 14, complete your current signature sweep and write the report. If you hit turn 18, STOP and emit a REVIEW coverage-gap finding for any attack signature category or environment not yet checked. Useful fields: source, message, application_name/program, facility, level, pid, and auditd fields auditd_type, auditd_exe, auditd_comm, auditd_uid, auditd_auid when present; fall back to message substring matching if a field is absent. Treat strings jndi and ceurufrd as allow-listed known, with ONE override: if a jndi line is paired with an active kill-chain signal on the same host within 60s (outbound LDAP/RMI, curl/wget to /tmp, reverse-shell pattern, new uid=0, or /etc/passwd write), override the allow-list and promote to HIGH. Correlation pass: two REVIEW signals on the same host+user or host+src_ip within 1 hour upgrade to HIGH. Aggregate first. Do NOT generate your own PDF or skill-native report - write ONLY the markdown file below, ending with the findings-json block. Save a concise markdown report to reports-noskill\linux-latest.md with evidence and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment (e.g. Azure on OP-GL), include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (iis|rdp|azure|linux), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields), detail (WHY this severity tier and why NOT higher or lower + how it was detected + analyst-handoff assessment; <=700 chars). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji.