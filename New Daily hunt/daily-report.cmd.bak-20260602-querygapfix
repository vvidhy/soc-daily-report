@echo off
cd /d "C:\Users\VidhyaV\soc-monitor"
echo ==== DAILY REPORT %DATE% %TIME% ==== >> "logs\daily.log"
type nul > "logs\run-start.flag"
start "SOC-hunt-watchdog" /b powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\VidhyaV\soc-monitor\hunt-watchdog.ps1"
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily IIS web-attack hunt for the last 24 hours. Use the iis-log-hunt skill if available; otherwise hunt directly with the Graylog MCP tools. SCOPE: run coverage across ALL FOUR environments (OP-GL, PROD-GL, DEV-GL, AZ-GL) using base filter filebeat_log_file_path:*inetpub* - do not restrict to OP-GL only; named OP-GL streams include IIS_PROD and IIS_UAT, plus other IIS/Rebex/Exolution web streams. IIS field schema (stay within these): Client_ip, URI_Stream, URI_Query, Status, UserAgent, Referer, Time_Taken, Client_Bytes, Server_Bytes, Port, Host, Method. Suppress internal Nessus/Tenable scans at query time via a NOT UserAgent filter matching Nessus*, the literal Nessus SOAP, and Tenable* - BUT before suppressing any scanner-looking finding, check URI_Query for .nasl plugin filenames (Nessus run with a spoofed UA) and keep the finding if present. Internal RFC-1918 source IPs are NOT automatically safe - treat them as possible threat actors. PARSER BLIND SPOT - CRITICAL (the IIS Graylog extractor is misaligned on 3 of 4 GLs, so named-field queries UNDER-REPORT and can falsely read clean): on PROD-GL, DEV-GL and AZ-GL the named fields Status, Method, URI_Stream, Client_ip and Host are frequently null or shifted right (sites emit extra W3C columns cs-host, X-Forwarded-For, cs-username the extractor was not built for, and some FOIA-API hosts omit s-sitename and miss entirely), so a named-field query returning zero is NOT proof of clean on those three GLs. Do NOT trust a clean named-field result on PROD/DEV/AZ. Hunt PRIMARILY on the raw full-text message field (present across all envs) using attack-token signatures (message contains UNION SELECT, xp_cmdshell, information_schema, the percent-encoded dot-dot traversal sequence, encoded script tags, cmd.exe, etc/passwd, win.ini, sqlmap, nikto), AND additionally run a coverage sweep for extractor-dropped events via NOT _exists_:Status on PROD/DEV/AZ to catch unparsed traffic. When you need the real client IP, derive it from the FIRST X-Forwarded-For token inside message, not c-ip (c-ip is always the 10.x KEMP load-balancer). OP-GL parses correctly and may use named fields. The durable extractor fix is documented in D:\Vidhya\iis-extractor-fix.md. PUBLIC-FACING SCAN AND ENUMERATION COVERAGE - MANDATORY (especially OP-GL, whose sites like FOIAXpress doed.secureocp.com and PAL are internet-exposed and attract continuous opportunistic scanning - a near-empty IIS result for OP-GL almost always means this coverage was skipped, NOT that the estate is quiet): in ADDITION to the injection signatures you MUST run and report these three sweeps. (1) AUTOMATION/SCANNER USER-AGENTS - aggregate by UserAgent and flag automated-tool agents far beyond Nessus/Tenable: python-requests, Go-http-client, curl, wget, libwww, fasthttp, okhttp, masscan, zgrab, zmeu, nuclei, nikto, sqlmap, acunetix, nmap, WPScan, CensysInspect, Censys, Shodan, InternetMeasurement, Expanse, and any non-browser library UA hitting many distinct paths. (2) 404 ENUMERATION - aggregate Status 404 by Client_ip and flag external source IPs (exclude RFC-1918, the three Cato egress IPs, and Zscaler proxy ranges 136.226.x and 165.225.x which are benign corporate egress) that generate high 404 fan-out across many distinct URIs. (3) CMS/CVE PROBE PATHS - search URI_Stream for opportunistic probe paths: wp-login.php, xmlrpc.php and its /blog/ /wp/ /wordpress/ /cms/ fan-out, dot-env files, dot-git directories, actuator, vendor phpunit eval-stdin, cgi-bin, HNAP1, boaform, config/ses.php, struts action, and shell/upload extensions. REPORTING RULE: characterize opportunistic external scanning as ONE rolled-up finding PER ENVIRONMENT - severity LOW when everything was 404 or blocked (background internet noise, still reported, never dropped as benign), escalate to REVIEW or HIGH only if a probe touched a sensitive or known-vulnerable path OR returned a non-404 success. Do NOT emit one finding per scanner IP (that floods the report); instead give counts inside the single rolled-up entry (total scanner-UA requests, total 404s, distinct external source IPs, the top offenders, and the notable probe paths). Look for SQLi, XSS, path traversal, webshell, RCE, LFI/RFI, scanners, credential stuffing, SSRF, beaconing, web exfil. Aggregate first; do not pull millions of rows. Do NOT generate your own PDF or skill-native report - write ONLY the markdown file below, ending with the findings-json block. Save a concise markdown report to reports\iis-latest.md with findings, severity, evidence (timestamp, source IP, URL, status) and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment (e.g. Azure on OP-GL), include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (iis|rdp|azure|linux), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model opus --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily WINDOWS + RDP hunt for the last 24 hours across all four Graylogs (Winlogbeat: AZ-GL Winlog-Beat, PROD-GL Winlog_beat, DEV-GL Winlogbeat, OP-GL Winlog_beat). Use BOTH the rdp-windows-correlation-hunt skill (RDP session kill-chain) and the windows-event-hunt skill (host-wide event-centric sweep, especially Defender detections) if available; otherwise hunt directly via MCP. Do BOTH halves in ONE pass over the shared Winlogbeat data: (i) the session-centric RDP walk for interesting RDP sessions plus the behavioral triggers, AND (ii) the host-wide EVENT-CENTRIC sweep described below, which runs regardless of whether any RDP session occurred (this is the half a pure RDP hunt misses). STREAM IDs are pre-resolved in C:\Users\VidhyaV\soc-monitor\streams.json (by environment, title to id) - read it once and use those IDs to scope your searches directly; do NOT call list_streams. Fall back to list_streams only for a stream missing from that file or if a listed ID returns zero rows. FIELD CONVENTION (critical - wrong names return zero silently): use underscored Winlogbeat fields winlogbeat_winlog_event_id, winlogbeat_event_data_* (e.g. winlogbeat_event_data_TargetUserName, winlogbeat_event_data_IpAddress, winlogbeat_event_data_WorkstationName), winlogbeat_winlog_channel - NOT dotted winlog.event_id. Key event IDs: 4624 logon, 4625 failed logon, 4672 special privileges, 4688 process create, 4698/4702 scheduled task, 7045/4697 service install, 4720 new account, 4728/4732/4756 group add, 4719 audit policy change, 1102 security log cleared, 4648 explicit creds, 4776 NTLM, 5140/5145 share access, 4769 Kerberos (RC4 type 0x17 = Kerberoast), 4778/4779 RDP reconnect. Decode 4625 SubStatus: 0xC000006A bad password, 0xC0000234 locked, 0xC0000064 unknown user, 0xC000006F outside hours, 0xC0000072 disabled. Thresholds: failed-login burst = more than 3x 4625 by one user; late-night = 22:00-06:00 local; rapid hopping = under 5 min between different workstations. Escalate immediately (do not wait for the report): 1102 log clear; 4720+4728 by the same user within 10 min; 4769 RC4; 4624 type 10 to a Domain Controller from a non-admin IP; 4778 from a different client than the preceding 4779; PowerShell v2 downgrade; over 100 unique 5145 file accesses in under 10 min. Look for RDP brute force, failed-login bursts, late-night logons, tscon hijack, rapid workstation hopping, lateral movement, new admins. MANDATORY BEHAVIORAL CORRELATION (B1-B4) - this is the session-behavioral half of the hunt and you MUST compute and report ALL of it explicitly; do NOT collapse the RDP surface to a CLEAN verdict before you have actually run B1 through B4. B1 failed-login burst: aggregate 4625 by TargetUserName and list every user with more than 3 failures together with their decoded SubStatus reason. B2 failed-then-succeeded: for each B1 user that later shows a 4624 success, trace the post-login activity (4688 process, 7045 service, 5145 share) and give a verdict. B3 late-night / after-hours: aggregate ALL 4624 type-10 by hour-of-day and flag every session between 22:00 and 06:00 local time; group the flagged users into named cohorts (for example an offshore team logging in during their own daytime is benign but MUST STILL be reported as a named B3 cohort with a recommended after-hours allow-list, never dropped or folded into CLEAN). If the hour-of-day field is absent on a GL, derive the hour from the event timestamp instead and state the partial coverage - never skip B3. B4 rapid workstation hopping: flag any user with 4624 logons to different workstations less than 5 minutes apart. For EVERY behavioral trigger that actually fires (B1, B2, B3, B4), you MUST emit a SEPARATE findings-json entry with surface rdp that names the user or cohort and the trigger, even when the verdict is benign - use LOW or REVIEW, never silently omit a fired trigger. Inside each environment's section include a one-line B1-B4 summary (counts per trigger) so the behavioral queue is visible even on a clean day. EVENT-CENTRIC BROAD SWEEP (run host-wide via aggregate_logs - counts by host/user/event_id first, then drill down on anomalies capped at 10 rows - NOT gated on an RDP session): (1) DEFENDER/AV - query winlogbeat_winlog_channel:\"Microsoft-Windows-Windows Defender/Operational\" for event IDs 1006 1007 1008 1015 1116 1117 1118 1119 5001 5007 5010 5012 - ANY malware/threat detection, failed-to-clean/quarantine, real-time-protection-disabled, or tamper-protection-off is at least HIGH; report host, threat name, action, and whether it was cleaned; (2) ACCOUNT MGMT - 4720 new account, 4722/4725/4726, 4738 change, 4767 unlock, 4781 rename, and group adds 4728/4732/4756 ESPECIALLY to Administrators/Domain Admins/Enterprise Admins - flag regardless of how the actor logged in; (3) SERVICE CREATION - 7045 (System channel) and 4697 (Security) - new service with a LOLBin or suspicious binary path; (4) PROCESS EXECUTION - 4688 LOLBins on ANY host (powershell -enc, certutil, rundll32, regsvr32, mshta, wmic, bitsadmin, psexesvc); (5) NTLM - 4776 failures/anomalies and relay patterns; (6) SYSTEM TAMPERING - 1102 Security-log cleared, 104 System-log cleared, 4719 audit-policy change, 4616 large time change. These six are host-wide and do NOT require a preceding RDP session. Label each finding's surface as one of defender|account|service|process|ntlm|system (use rdp for the RDP session-walk findings) - ignore the generic surface list further below and use these. Aggregate first. Do NOT generate your own PDF or skill-native report - write ONLY the markdown file below, ending with the findings-json block. Save a concise markdown report to reports\rdp-latest.md with evidence and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment (e.g. Azure on OP-GL), include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (iis|rdp|azure|linux), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model opus --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
echo ===AZURE-OUTPUT-START=== >> "logs\daily.log"
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily Entra/Azure identity hunt for the last 24 hours. Use the azure skill if available; otherwise query the Azure Event Hub streams across environments via MCP. TENANT-TO-STREAM ROUTING (one stream per tenant): mycasepoint.com -> AZ-GL stream 65cf0501c0fccf5d65f5025f; casepoint.com -> PROD-GL stream 6979bcc8245e61e78824df15; casepoint.in -> DEV-GL stream 69fb008584d94819ed097db0; OP-GL has NO Azure stream - do NOT run ANY query against OP-GL for Azure (it would only waste tokens); simply emit the ## OP-GL section as Not applicable. FIELD NAMES are real misspellings emitted by the content pack - use them verbatim or queries return zero silently: azure_prob_app_disply_name (disply not display), azure_prob_condi_access_status (condi not condit), azure_prop_resource_disply_name; the parser emits both azure_prob_* and azure_prop_* prefixes so OR them together (e.g. azure_prop_user_principal_name OR azure_prob_user_principal_name). IGNORE these Casepoint Cato egress IPs (they are not attackers): 140.82.202.196, 199.27.40.187, 123.253.153.138. Token-replay tell: MFA satisfied by claim in token from a new IP. Note azure_prob_device_detail_is_compliant is broken (false for ~99.6% of sign-ins) - do not rely on it for device posture. Look for risky sign-ins, MFA failure/bypass, impossible travel, token replay, cross-tenant access, conditional-access anomalies, new admin/role changes. Suppress noise codes 70043, 70044, 50173, 50140. Aggregate first. Do NOT generate your own PDF or skill-native report - write ONLY the markdown file below, ending with the findings-json block. You MUST call the Write tool to SAVE the full report to reports\azure-latest.md (overwrite any existing file) BEFORE you finish - do NOT only print it to the screen; the task is NOT complete until that file exists on disk, and printing the report instead of writing it is a failure. Save the markdown report to reports\azure-latest.md with evidence and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment (e.g. Azure on OP-GL), include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (iis|rdp|azure|linux), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model opus --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
powershell -NoProfile -ExecutionPolicy Bypass -Command "$log=Get-Content 'C:\Users\VidhyaV\soc-monitor\logs\daily.log' -Raw; $i=$log.LastIndexOf('===AZURE-OUTPUT-START==='); $az=if($i -ge 0){$log.Substring($i)}else{''}; $f='C:\Users\VidhyaV\soc-monitor\reports\azure-latest.md'; $flag=(Get-Item 'C:\Users\VidhyaV\soc-monitor\logs\run-start.flag').LastWriteTime; if((-not (Test-Path $f)) -or ((Get-Item $f).LastWriteTime -lt $flag)){ if($az -match 'findings-json'){ [IO.File]::WriteAllText($f,$az); Write-Output 'AZURE FALLBACK: rebuilt azure-latest.md from captured log output' } else { Write-Output 'AZURE FALLBACK FAILED: no findings-json in azure output - manual check needed' } } else { Write-Output 'Azure wrote azure-latest.md normally' }" >> "logs\daily.log" 2>&1
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily Linux hunt for the last 24 hours across all four Graylogs (Rsyslog/syslog/auth/secure/audit streams). Use the sd-linux skill if available; otherwise hunt directly via MCP. STREAM IDs are pre-resolved in C:\Users\VidhyaV\soc-monitor\streams.json (by environment, title to id) - read it once and use those IDs to scope your searches directly; do NOT call list_streams. Fall back to list_streams only for a stream missing from that file or if a listed ID returns zero rows. Look for sshd brute force, sudo abuse, reverse shells, cryptominers (xmrig, kdevtmpfsi, kinsing, sysrv), cron/systemd/authorized_keys persistence, /etc/passwd|shadow|sudoers tampering. Useful fields: source, message, application_name/program, facility, level, pid, and auditd fields auditd_type, auditd_exe, auditd_comm, auditd_uid, auditd_auid when present; fall back to message substring matching if a field is absent. Treat strings jndi and ceurufrd as allow-listed known, with ONE override: if a jndi line is paired with an active kill-chain signal on the same host within 60s (outbound LDAP/RMI, curl/wget to /tmp, reverse-shell pattern, new uid=0, or /etc/passwd write), override the allow-list and promote to HIGH. Correlation pass: two REVIEW signals on the same host+user or host+src_ip within 1 hour upgrade to HIGH. Aggregate first. Do NOT generate your own PDF or skill-native report - write ONLY the markdown file below, ending with the findings-json block. Save a concise markdown report to reports\linux-latest.md with evidence and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment (e.g. Azure on OP-GL), include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (iis|rdp|azure|linux), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model opus --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily SFTP and DTC hunt for the last 24 hours. Use the sftp-graylog-hunt skill's detection logic if available; otherwise hunt directly via MCP. STREAM IDs are pre-resolved in C:\Users\VidhyaV\soc-monitor\streams.json (by environment, title to id) - read it once and use those IDs to scope your searches directly; do NOT call list_streams. Fall back to list_streams only for a stream missing from that file or if a listed ID returns zero rows. SFTP streams are named differently per Graylog - use the right names: PROD-GL = External_SFTP_cerebus + Internal_SFTP_cerebus; OP-GL = External_SFTP + Internal_SFTP; DEV-GL = single combined stream SFTP_cerebus; AZ-GL has NO SFTP streams - do NOT run ANY query against AZ-GL for SFTP (it would only waste tokens); simply emit the ## AZ-GL section as Not applicable. DTC = Rebex streams Rebex_PROD, Rebex_CA, Rebex_PT. ALLOWLIST - these three are Casepoint Cato egress IPs, NOT external attackers: 140.82.202.196, 199.27.40.187, 123.253.153.138. When any appears as the client/source IP, cap severity at MEDIUM and tag the finding 'Cato egress - likely false positive'; do not silently drop it if a real attack pattern is present (webshell path, executable upload, credential filename), but never treat a Cato IP as a confirmed external threat. Cerberus SFTP message decoding: 'Successfully stored file at' = upload (client to server), 'Successfully sent file' = download (server to client), 'Key exchange failed' or 'SSL accept error' or 'wrong version number' = SSH/TLS scanner probe; the username appears after the IP in brackets and session id ties events together. Useful fields: Client_ip (remote IP), Origin_ip (ASN/geo string), Size_In_MB (0 means non-transfer event). Look for SFTP brute force, failed-login spikes, large transfers / exfil, suspicious file extensions, malware or executable drops, anomalous file-share behavior. For auth failures, distinguish BURST (many failures within 60s from one IP = brute force, higher severity) from SCATTERED (spread over hours = low-and-slow or noise, lower severity); require a correlated second indicator before calling anything HIGH. Aggregate first; do not pull millions of rows. Do NOT generate the skill's own PDF - instead save a concise markdown report to reports\sftp-latest.md with findings, severity, evidence (timestamp, source IP, file, bytes) and MITRE IDs. Structure the markdown with exactly 4 sections, one per environment, each starting on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL. If a surface does not apply to an environment, include the section header with `Not applicable` or `No findings`. Do not add other top-level ## headers. After the per-env sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array of finding objects, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired). Never silently drop a finding; include LOW and REVIEW entries in the findings-json, not just HIGH/MEDIUM. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (sftp|dtc), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model opus --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily NETWORK infrastructure hunt for the last 24 hours across all four Graylogs - FIREWALL, SWITCHES and LOAD BALANCER only. Hunt directly via MCP. STEP 1: Read C:\Users\VidhyaV\soc-monitor\infra-streams.json and use ONLY the firewall, switch and lb categories - query the listed stream IDs directly, do NOT call list_streams; a GL not listed for a category is Not Applicable there. TOKEN + LATENCY BUDGET (these streams are huge and slow to scan): every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per category per GL, plus ONE <=10-row drill-down per CONFIRMED HIGH only. Narrow index-friendly filters: FIREWALL (Fortigate, the slow one - keep to this single indexed clause) -> action:deny only; SWITCHES -> link-change OR auth-failure OR config-change; LOAD BALANCER (KEMP) -> login OR permission-change OR config-change OR certificate-change. If a category's aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding 'NOT ASSESSED this run - <category> coverage gap', action 're-run manually') and MOVE ON; NEVER record a slow or skipped category as No findings. WHAT TO HUNT: FIREWALL - denied/blocked spikes from one external IP, port-scan fan-out, IPS/IDS signature hits, geo-anomalous allows, VPN or admin auth failures, config changes, any inbound allow to mgmt ports 22/3389/445 from external IPs. SWITCHES - link up/down flapping, running-config changes, repeated mgmt auth failures, port-security violations, unexpected SNMP or admin access. LOAD BALANCER (KEMP) - admin logins and failures, virtual-service or rule changes, certificate changes, TLS handshake error spikes, health-check state flapping. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag 'Cato egress - likely false positive'; internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports\network-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with exactly 4 sections, one per environment, each on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL; under each note each category checked with No findings or Not applicable. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (one of firewall|switch|lb), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model sonnet --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily DATA-SERVICES (DB) hunt for the last 24 hours across all four Graylogs - Postgres/pgbouncer, Redis, RabbitMQ and Elasticsearch/Opensearch only. Hunt directly via MCP. STEP 1: Read C:\Users\VidhyaV\soc-monitor\infra-streams.json and use ONLY the db category - query the listed stream IDs directly, do NOT call list_streams; a GL not listed is Not Applicable there (OP-GL db = Elasticsearch, with Opensearch as the live fallback). TOKEN + LATENCY BUDGET (these are the highest-volume, slowest streams - e.g. RabbitMQ ~2.3M/day): every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per stream per GL, plus ONE <=10-row drill-down per CONFIRMED HIGH only. Narrow index-friendly filters: RabbitMQ -> auth-failure / access-refused only; Redis -> the dangerous commands CONFIG|SLAVEOF|FLUSHALL|MODULE or auth errors only; Postgres/pgbouncer -> FATAL auth or role errors only; Elasticsearch and Opensearch -> error OR FATAL only. If a stream's aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding 'NOT ASSESSED this run - <stream> coverage gap', action 're-run manually') and MOVE ON; NEVER record a slow or skipped stream as No findings. WHAT TO HUNT: auth failures / brute force against DB, queue or search, connection floods, Redis unauthenticated dangerous commands (CONFIG/SLAVEOF/MODULE/FLUSHALL), Postgres FATAL auth or role errors, error or FATAL spikes suggesting injection or scanning, config changes, unexpected admin access. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag 'Cato egress - likely false positive'; internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports\db-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with exactly 4 sections, one per environment, each on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL; under each note each stream checked with No findings or Not applicable. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (always the literal db), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model sonnet --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a daily INFRA security-appliance hunt for the last 24 hours across all four Graylogs - EDR (ESET), MFA (Securenvoy), EXFIL monitor, and VIRTUALIZATION / OUT-OF-BAND MGMT (vCenter/ESXi/Dell-iLO/UCS) only. Hunt directly via MCP. STEP 1: Read C:\Users\VidhyaV\soc-monitor\infra-streams.json and use ONLY the edr, mfa, exfil and virt categories - query the listed stream IDs directly, do NOT call list_streams; a GL not listed for a category is Not Applicable there (EXFIL = Outgoing Traffic Monitor, DEV-GL only, event-driven, empty is normal). TOKEN + LATENCY BUDGET: every query MUST be a single aggregate_logs call with an explicit small limit - never raw rows, never aggregate over * , never broad free-text. HARD CAP: at most ONE aggregate_logs per category per GL, plus ONE <=10-row drill-down per CONFIRMED HIGH only. Narrow index-friendly filters: EDR (ESET) -> threat/detection/quarantine/infiltration events only; MFA (Securenvoy) -> failure/denied/lockout only; EXFIL -> threshold-breach OR over-quota alerts only; VIRT and OOB -> login OR permission-change OR VM-lifecycle OR config-change. If a category's aggregate does not return promptly, do NOT mark it clean - emit a REVIEW finding (finding 'NOT ASSESSED this run - <category> coverage gap', action 're-run manually') and MOVE ON; NEVER record a slow or skipped category as No findings. WHAT TO HUNT: EDR-ESET - ANY malware or threat detection, quarantine, real-time-protection-disabled, or threat-not-cleaned event is at least a HIGH candidate; report host, threat name, action, and whether it was cleaned. MFA-Securenvoy - auth failures, repeated denials, lockouts, token or seed errors, admin changes. EXFIL - any threshold-breach or over-quota alert is an exfil candidate; name the host and volume. VIRTUALIZATION-OOB - interactive or failed admin/root logins to vCenter/ESXi/iLO/UCS from unexpected IPs, VM create/delete/clone/export, datastore browse or download, account or permission changes, SSH enabled on ESXi. ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 cap at MEDIUM and tag 'Cato egress - likely false positive'; internal RFC-1918 IPs are NOT automatically safe. You MUST use the Write tool to SAVE the report to reports\infra-latest.md before finishing - printing instead of writing is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with exactly 4 sections, one per environment, each on its own line: ## AZ-GL, ## PROD-GL, ## DEV-GL, ## OP-GL; under each note each category checked with No findings or Not applicable. Then on a new line append a fenced code block: three backticks then findings-json (so it reads as ```findings-json), a valid JSON array, then closing three backticks. One object per distinct finding (include a CLEAN entry per env if nothing fired); never silently drop a finding; include LOW and REVIEW. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (AZ-GL|PROD-GL|DEV-GL|OP-GL), surface (one of edr|mfa|exfil|virt), finding (<=120 chars), evidence (<=100 chars - timestamps, IPs, counts), mitre (T-code list or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query you actually ran), investigate (a concrete Graylog query to run NEXT - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model sonnet --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\VidhyaV\soc-monitor\prep-correlation.ps1" >> "logs\daily.log" 2>&1
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\VidhyaV\soc-monitor\correlation-gate.ps1" >> "logs\daily.log" 2>&1
if not errorlevel 1 goto skipcorrelation
call "C:\Users\VidhyaV\AppData\Roaming\npm\claude.cmd" -p "Run a CROSS-MODULE correlation and kill-chain reconstruction pass for today's daily hunt. Do NOT re-hunt raw logs broadly. INPUT: read the single file reports\_merged-findings.json - a compact JSON array of EVERY finding from all 6 hunts (each object has sev, env, surface, finding, evidence, mitre, action). This merged file is your ONLY input; do NOT read the per-hunt markdown files (saves tokens). Build an entity index across ALL findings: source/client IPs, usernames/accounts, hostnames/workstations, and timestamps. GOAL: find attack chains that span more than one surface or more than one environment - the thing single-surface hunts cannot see. CORRELATE when two or more findings from DIFFERENT surfaces (iis/rdp/azure/linux/sftp/firewall/edr/mfa/exfil/db/virt/switch/lb) or different envs share an entity (same IP, same username, same host) OR cluster in a tight time window on the same host/env. For each correlation, reconstruct the kill chain and map each step in order to a MITRE ATT&CK tactic (Reconnaissance, Initial Access, Execution, Persistence, Privilege Escalation, Defense Evasion, Credential Access, Discovery, Lateral Movement, Collection, Command and Control, Exfiltration, Impact). ALLOW-LIST: the three Casepoint Cato egress IPs 140.82.202.196, 199.27.40.187, 123.253.153.138 are shared corporate egress, NOT attacker pivots - never anchor a kill chain on them. CONFIRMATION (optional, bounded): you MAY run at most 5 targeted aggregation queries via the Graylog MCP to confirm a HIGH-confidence suspected link (e.g. whether one source IP appears across two surfaces on the same host in the window) - aggregate only, never pull raw rows, never exceed 5 queries total. SEVERITY: a confirmed multi-stage chain spanning surfaces = HIGH; a plausible but unconfirmed shared-entity link = REVIEW; a weak or coincidental overlap = LOW. If NO cross-surface or cross-env chain exists, say so explicitly. You MUST use the Write tool to SAVE the report to reports\correlation-latest.md (overwrite any existing file) BEFORE finishing - the task is NOT complete until that file exists on disk; printing it instead of writing it is a failure. Do NOT generate your own PDF or skill-native report. Structure the markdown with these sections: '## Kill Chains' (one subsection per reconstructed chain with the ordered MITRE stages, the shared entity, the contributing findings by surface and env, and a confidence level), '## Correlated Entities' (the IPs/users/hosts that appeared in more than one finding), and '## Cross-Check Coverage' (which entities were cross-checked and what was ruled out). After those sections, on a new line, append a fenced code block: three backticks followed by findings-json (so it reads as ```findings-json), then a valid JSON array, then closing three backticks. One object per reconstructed chain (include one CLEAN object if no cross-surface chain was found). Never silently drop a correlation; include LOW and REVIEW entries too. Keys: sev (HIGH|MEDIUM|REVIEW|LOW|CLEAN), env (the environment(s) involved, e.g. OP-GL or OP-GL+DEV-GL), surface (always the literal correlation), finding (<=120 chars - the chain summary), evidence (<=100 chars - shared entity plus contributing surfaces plus timestamps), mitre (T-code list across the chain or empty), action (<=100 chars concrete next step), query (the exact Graylog/Elasticsearch query string you actually ran to surface this finding), investigate (a concrete Graylog query the analyst should run NEXT for deeper investigation - pivot on the IP, user, or host and name the stream/fields). Plain ASCII only inside JSON strings; no smart quotes, em-dashes, or emoji." --model sonnet --permission-mode bypassPermissions --mcp-config "C:\Users\VidhyaV\soc-monitor\.mcp.json" --strict-mcp-config --max-turns 200 < NUL >> "logs\daily.log" 2>&1
:skipcorrelation
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\VidhyaV\soc-monitor\generate-pdf.ps1" >> "logs\daily.log" 2>&1
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\VidhyaV\soc-monitor\send-report.ps1" >> "logs\daily.log" 2>&1
echo. >> "logs\daily.log"
